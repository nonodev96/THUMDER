.DATA 0
.DOUBLE 16, 13, 18, 10, 12, 15, 17, 18, 15, 12, 11, 18
.GLOBAL CONST
CONST: .DOUBLE 14

.TEXT 256

LD          F0, CONST
ADD         R1, R0, R0; PUNTERO
ADDI        R2, R0, 12; CONTADOR DECREMENTAL PARA BUCLE
ADD         R5, R0, R0; CONTADOR DE LOS DÍAS EN LOS QUE LA TEMPERATURA ES MAYOR A 14 GRADOS


BUCLE:	
    LD      F2, 0(R1)
	GTD     F2, F0
	BFPT    INCREMENTO

SIGUE:	
    ADDI    R1, R1, 8
	SUBI    R2, R2, 1
	BNEZ    R2, BUCLE
	TRAP    #0

INCREMENTO:
	ADDI    R5, R5, 1
	J       SIGUE
